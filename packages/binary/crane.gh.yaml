src: https://github.com/google/go-containerregistry/releases/download/v0.21.6/go-containerregistry_Linux_x86_64.tar.gz
dst: /usr/local/bin/crane
process: "mkdir -p $tmpdir/unpacked && tar -xvzf $tmpfile -C $tmpdir/unpacked/ && mv $tmpdir/unpacked/crane $tmpfile"
